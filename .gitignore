# Parent-level ignores only. The web app keeps its own web/.gitignore, and each
# plugin submodule keeps its own (build/ artefacts live there and are ~400MB each).

.DS_Store
*.pem

# Editor / tooling state
.idea/
.vscode/
.claude/settings.local.json
